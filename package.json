{
  "name": "cronos",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --filter '*' dev"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  }
}
